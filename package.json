{
  "name": "recipes-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
