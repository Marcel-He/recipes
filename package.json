{
  "name": "recipes-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "precommit": "prettier --write 'src/**/*.scss'"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@fortawesome/free-solid-svg-icons": "^7.2.0",
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "sass": "^1.101.0",
    "scss": "^0.2.4",
    "vitest": "^2.0.0"
  }
}
